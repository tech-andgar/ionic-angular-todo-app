import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'me.techandgar.demo.nequi.todo',
  appName: 'Nequi TO-DO',
  webDir: 'www'
};

export default config;
